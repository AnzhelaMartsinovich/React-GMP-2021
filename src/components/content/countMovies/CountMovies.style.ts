import { styled, theme } from 'theme';

export const CountMoviesContainer = styled.div`
  color: ${theme.colors.white};
  font-size: 20px;
  letter-spacing: 1px;
`;

export const CountMoviesNumber = styled.p`
  display: inline-block;
  margin-right: 5px;
  font-size: 23px;
  font-weight: 600;
`;
